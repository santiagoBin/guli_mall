spring:
  application:
    name: gulimall-cart
  cloud:
    nacos:
      discovery:
        server-addr: nacos-service.gulimall:8848
#配置线程池
gulimall:
  thread:
    coreSize: 36
    maxSize: 200
    keepAliveTime: 10
